// styleMap задающий дополнительные (custom) inlineStyles

const styleMap = {
  // стиль используется для сохранения отображения selection
  // при потере фокуса редактором, например, при активизации Popover
  FAKESELECTION: {
    background: '#B3D4FB',
  },
};

export default styleMap;
